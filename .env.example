# Production domain used for absolute URLs in metadata (Open Graph image,
# sitemap.xml, robots.txt). Falls back to a placeholder example.com domain
# if unset — set this in your deployment environment once a real domain
# is assigned.
NEXT_PUBLIC_SITE_URL=https://kembangbalihomespa.example.com
